import {version as VERSION} from '../package.json';

const plugin = function (options) {
  // something to
};

plugin.id = Math.floor((Math.random() * 99999999) + 1);

plugin.VERSION = VERSION;


export default plugin;
